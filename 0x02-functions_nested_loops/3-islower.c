#include "main.h"
/**
 * _islower - function that checks for lowercase
 * @c: the carctor to be checked
 * Return: 1 for lowercase carctor or  0 for any thing else
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{

	return (1);
	}
	return (0);
}
